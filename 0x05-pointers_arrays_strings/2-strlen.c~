#include "holberton.c"
#include <stdio.h>
/**
 *_strlen - to return the length of string
 * @s: parameter
 * Return: length of string
 */
int _strlen(char *s)
{
int i = 0;
while (s[i])
i++;
return (i);
}
